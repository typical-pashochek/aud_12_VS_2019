1.В файле x86.c приведены примеры реализаций функций с переменным числом параметров в Си (функция принимает число параметров, сами параметры, вычисляет их сумму):
1)"Неправильный" способ через блуждание по стеку с помощью арифметики указателей
2)Правильный способ через <stdarg.h>
скомпилировать под x86, запустить, проверить
скомпилировать под x64, запустить, проверить

Написать свою реализацию таких же функций для типа double: sumDouble и sum_double_via_stdargMACROSES

Написать свою функцию concat, объединяющую любое количество строк в одну.
Вариант вызова:
	char* str1 = concat("one ", "two ", "three ", "four", "\0");
	printf("%s\n",str1);
	free(str1);
	str1 = concat("Happy ", "new ", "year!", "\0");
	printf("%s\n", str1);
	free(str1);
	str1 = concat("Hello ", "world!", "\0");
	printf("%s\n", str1);
	free(str1);